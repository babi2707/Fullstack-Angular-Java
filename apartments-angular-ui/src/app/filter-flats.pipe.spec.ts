import { FilterFlatsPipe } from './filter-flats.pipe';

describe('FilterFlatsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterFlatsPipe();
    expect(pipe).toBeTruthy();
  });
});
